Nightjar Capabilities
Copyright 2026 BlackKrait Technologies

This product includes software extracted and modified from Row-Bot
(https://github.com/siddsachar/row-bot), used under the Apache License, 2.0.

The following NOTICE is preserved from the original Row-Bot distribution, per
Apache License 2.0 section 4(d):

    ------------------------------------------------------------------------
    Row-Bot
    Copyright 2026 Row-Bot Contributors

    Licensed under the Apache License, Version 2.0.
    ------------------------------------------------------------------------

Modifications made by BlackKrait Technologies for the Nightjar project
(Apache License 2.0 section 4(b)): the voice (faster-whisper / Kokoro),
vision, hybrid graph+vector memory, and Playwright browser modules were
extracted from Row-Bot and modified to remove all LangChain / LangGraph /
NiceGUI dependencies and orchestration, to run headless, and to be exposed
through a Model Context Protocol (MCP) server plus a WebSocket side-channel.
The embedding backend was changed from HuggingFace/torch to a local Ollama
embedding model. The TTS grapheme-to-phoneme layer was additionally changed
from kokoro-onnx's espeak-ng tokenizer to misaki (see below). Each derived
source file carries a header noting it is a modified extract of the
corresponding Row-Bot source.

The full text of the Apache License 2.0 governing the Row-Bot-derived portions
is included in this package as LICENSE.row-bot.

------------------------------------------------------------------------
Text-to-speech components
------------------------------------------------------------------------

Kokoro-82M model weights (kokoro-v1.0.fp16.onnx, voices-v1.0.bin)
    Copyright 2024 hexgrad — Apache License 2.0.
    Downloaded at runtime to the user's model cache; not vendored here.

misaki — grapheme-to-phoneme for Kokoro
    Copyright 2025 hexgrad — Apache License 2.0.
    Used as the sole G2P. Its optional espeak fallback (`misaki.espeak`,
    which requires the GPL phonemizer + espeak-ng) is NEVER imported;
    `nightjar_capabilities/tts_g2p.py` supplies a non-GPL fallback instead.

Kokoro phoneme vocabulary (`nightjar_capabilities/kokoro_vocab.json`)
    Extracted verbatim from kokoro-onnx's `DEFAULT_VOCAB`
    (https://github.com/thewh1teagle/kokoro-onnx), Copyright (c) 2024
    thewh1teagle — MIT License. Vendored as data so the kokoro-onnx package
    (whose Tokenizer loads a GPL espeak-ng binary via ctypes) is not a
    dependency.

en_core_web_sm — spaCy English pipeline used by misaki for POS tagging
    Copyright 2016 ExplosionAI GmbH — MIT License.

Nightjar previously depended on phonemizer-fork (GPL-3.0-or-later) and
espeakng-loader (a compiled espeak-ng binary shipped with its license notices
stripped) via kokoro-onnx. Both have been removed; see
`tests/test_tts_no_gpl.py` for the regression guard that keeps them out.

------------------------------------------------------------------------
Wake-word components (voice-phase PR 5)
------------------------------------------------------------------------

hey-buddy — wake-word training pipeline + JS reference implementation
    Copyright benjamin-paine (https://github.com/painebenjamin/hey-buddy)
    Apache License 2.0. Vendored at `wakeword_training/heybuddy_vendor/`
    (pinned commit, unmodified; see VENDOR.md there). The runtime detector
    (`nightjar_capabilities/wakeword.py`) is a Nightjar-authored onnxruntime
    port of its JavaScript inference path.

Wake-word ONNX artifacts (`nightjar_capabilities/models/wakeword/`)
    mel-spectrogram.onnx — torchlibrosa lineage (Copyright Qiuqiang Kong),
        ONNX export by benjamin-paine — Apache License 2.0.
    speech-embedding.onnx — Google's speech_embedding model, Copyright
        Google LLC — Apache License 2.0
        (https://www.kaggle.com/models/google/speech-embedding); ONNX
        conversion distributed by benjamin-paine. The distributing
        HuggingFace repo is tagged CC-BY-4.0 — attribution herewith.
    hey-buddy.onnx — Copyright benjamin-paine, Apache-2.0/CC-BY-4.0 per
        upstream; INTERIM stand-in only (see KNOWN_ISSUES.md NJ-59 and
        `model_licenses.json` for why it must not ship in a paid build).

Nightjar previously depended on openwakeword (code Apache-2.0), whose bundled
pretrained models are CC-BY-NC-SA 4.0 (non-commercial). The package and its
models have been removed (KNOWN_ISSUES.md NJ-58); see
`tests/test_model_licenses.py` for the regression guard that keeps them out.
